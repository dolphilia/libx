---
// シンプルなリダイレクト
import { getProjectConfig } from '@docs/project-config';
import { generateRedirectUrl } from '@docs/content-utils';

// 設定を初期化
const projectConfig = await getProjectConfig();

// サーバーサイドでは言語検出できないため、デフォルト言語を使用
const defaultLang = projectConfig.language.default;
// GitHub Pagesのベースパス
const baseUrl = projectConfig.paths.baseUrl;

// 実在する最新バージョンの最初のページへリダイレクト
const redirectUrl = await generateRedirectUrl(defaultLang, baseUrl);

// サーバーサイドでリダイレクト
return Astro.redirect(redirectUrl);
---
